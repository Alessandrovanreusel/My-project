# Deferred Work

## Deferred from: code review of story-1.1 (2026-06-04)

- **ObjectPool&lt;T&gt; hardening** [Assets/Scripts/Core/ObjectPool.cs] — `Get()` can return an instance Unity destroyed while it sat idle (scene unload) → `MissingReferenceException` on `SetActive(true)`; `Return()` has no null-check, no double-return detection (same object handed to two callers), no state reset on recycle, and accepts foreign objects; the constructor doesn't guard a null `_prefab` or negative `prewarm`; there's no `Clear()`/`Dispose()`, so the idle stack leaks across scene loads (contradicts the NFR3 "no leak" goal in its own doc-comment). **Defer reason:** no consumer yet — the correct guards depend on how `EventManager` uses the pool. Action: harden when implementing Story 1.6 (EventManager / event-actor lifecycle), against the real usage contract.
- **AnimHashes.Speed duplicates CharacterAnimator's private "Speed" hash** [Assets/Scripts/Core/AnimHashes.cs] — `AnimHashes.Speed` is currently unreferenced; the existing `CharacterAnimator` still computes its own `Animator.StringToHash("Speed")`. Two sources of truth for the same animator parameter. Deferred because the spec for Story 1.1 explicitly forbids modifying existing scripts. Action: when `CharacterAnimator` is migrated into the `CameraGame.Core` world in a later story, point it at `AnimHashes.Speed` and delete the local hash.

## Deferred from: play-test of story-1.2 (2026-06-05)

_Both found during Alexv's manual play-test. Out of scope for Story 1.2 (which only verifies traversal + camera + NavMesh on the existing imported world). Logged here for triage — likely a `correct-course` and/or new stories, possibly under Epic 2 (full map / art)._

- **See-through walls (camera sees behind/through wall geometry)** [Assets/Scenes/SampleScene.unity · Assets/Scripts/ThirdPersonCamera.cs] — In Play mode the player can see what's behind walls. Most likely cause(s): (a) imported wall meshes are **single-sided** (Blender exports often omit back faces — you see through the inside face), and/or (b) the first-person camera is nudged `+ transform.forward * 1f` with a near-clip of 0.3, so the camera origin can poke through thin walls when standing against them. **Action:** decide between an art fix (double-sided / thicker wall meshes) and a camera fix (near-clip / forward-offset tuning, or a head-position wall probe). Not a 1.2 AC — AC2 was only "only Main Camera renders" (satisfied).
- **Character (humanoid) is too tall relative to the world** [project-wide world scale] — The player is ~8.86 units tall because the whole project is modelled at a large, non-metric scale (documented in 1.2 Dev Notes as the biggest gotcha). "Too tall" vs buildings/doors is an **art/scale decision**, not a tweak: changing it ripples through the `CharacterController` dims, the head-bone FP camera, the ~16k MeshColliders, and the **NavMesh agent baked in Story 1.2**. **Action:** run `gds-correct-course` to decide whether to normalize world scale (and re-bake the NavMesh) or keep the current scale and adjust art. Explicitly NOT done in 1.2, which took the world as-is.

## Deferred from: code review of story-1.2 (2026-06-05)

_Three adversarial review layers ran; all 3 ACs satisfied (0 patches, 0 decisions). The two items below are real but pre-existing/theoretical — not blockers for 1.2. (A third deferred item, the AC2 re-import camera guard, is already tracked by the conditional subtask under Task 3 and is not duplicated here.)_

- **Sprint state not reset on focus-loss / `OnDisable`** [Assets/Scripts/ThirdPersonController.cs:151] — `_isSprinting` is only written inside `OnSprint`; there is no `OnApplicationFocus`/`OnDisable` reconciliation. Alt-tabbing away while holding Shift could leave sprint latched on with no key held. **Pre-existing** — not introduced by the `Button`→`Value` fix (which itself is correct). Minor; fold into a future controller-hardening pass (alongside any Story 1.6 input work).
- **NavMesh agent dimensions are aggressive vs. fine voxel geometry** [ProjectSettings/NavMeshAreas.asset] — radius 0.95 erodes walkable strips narrower than ~1.9u; climb 2 / height 8.86 against `cellSize` 0.16667 can in theory drop thin ledges or create overhang surfaces; openings with 2–8.86u clearance are walkable for the player but carved out for a NavMeshAgent. Harmless for the open, play-tested 1.2 patch. **Action:** re-verify when **Story 1.7's** drunk `NavMeshAgent` actually paths the world — and note this is entangled with the "character too tall / world scale" item above (a re-scale would force a NavMesh re-bake).

## Deferred from: code review of story-1.3 (2026-06-16)

_Three adversarial layers ran; all 3 ACs satisfied (0 decisions, 0 patches). One forward-looking item below; the rest of the layers' findings were dismissed (two "gamepad LT sticks" findings were false positives — that bug is specific to the `Button` action type, which the applied `Value` type fixes)._

- **Viewfinder `CanvasGroup.blocksRaycasts` / `interactable` never re-enabled in Photo mode** [Assets/Scripts/PhotoMode/PhotoModeController.cs:65-66] — `Awake` sets both `false` and only `alpha` is animated thereafter, so the overlay never blocks raycasts or becomes interactable even at full Photo mode. **Harmless for Story 1.3** — the viewfinder is an intentional non-interactive placeholder (`raycastTarget` off, no clickable elements). **Action:** when **Story 1.5 (capture)** adds interactive viewfinder elements (e.g. a shutter/focus reticle), flip these flags with the mode (true in Photo, false in Walk) — likely alongside the CameraConfig/zoom work folded in from 1.4.

## Deferred from: code review of story-1.4 (2026-06-17)

_Three adversarial layers ran; Acceptance Auditor found all 3 ACs + 7 guardrails satisfied (0 decisions; 1 patch offered for the inverted-FOV guard). The four items below are real but forward-looking — none is triggered by the current single-scene MVP. The Blind/Edge hunters' remaining findings were dismissed: the "RaiseCamera sticky" alarm was a false positive (it's a `Value` action, not `Button`); the every-frame FOV write in Walk and the once-per-press dpad stepping are both by-design per Guardrails 3 & 4; the frame-rate-dependent lerp is the exact formula Task 3 prescribed and passed the feel-test._

- **`photoCamera` not re-validated in `Update` — `MissingReferenceException` on runtime destruction** [Assets/Scripts/PhotoMode/PhotoModeController.cs:106-115] — `_zoomReady` is resolved once in `Awake` and never re-checked, so if `photoCamera` is destroyed during play (additive scene unload, camera swap) the per-frame `photoCamera.fieldOfView = ...` throws every frame, violating NFR8's "never throw in Update" to the letter. **No current trigger** — SampleScene is single-scene with the camera explicitly assigned. **Action:** add `if (photoCamera == null) { _zoomReady = false; return; }` at the top of the zoom block when additive scene loading arrives (Epic 5 — Home Base).
- **Zoom input desyncs from FOV while paused (`Time.timeScale == 0`)** [Assets/Scripts/PhotoMode/PhotoModeController.cs:113-114 vs OnZoom:146] — `OnZoom` still steps `_zoomT` while paused, but the FOV lerp is frozen (`Time.deltaTime == 0`), so accumulated zoom only snaps in on unpause. **No pause system exists yet.** **Action:** when a pause menu lands, either gate `OnZoom` on pause or drive the lerp with `unscaledDeltaTime` if zoom should respond while paused.
- **No `OnDisable` teardown** [Assets/Scripts/PhotoMode/PhotoModeController.cs] — disabling the controller mid-Photo leaves `Mode = Photo`, the viewfinder at its last alpha, and the camera FOV frozen at the last zoom. **No code path disables the controller today.** **Action:** add an `OnDisable` that restores Walk/wide FOV if the controller ever gets toggled (e.g. cutscenes, menus).
- **No scroll-inversion option** [Assets/Scripts/PhotoMode/PhotoModeController.cs:146] — `Mathf.Sign(y)` hard-codes scroll-up = zoom-in; OS "natural scrolling" users get the inverted feel with no toggle. **Fine for the solo MVP** (feel-tested on Alexv's setup). **Action:** add a `CameraConfig.invertZoom` bool and an options-menu binding if the game ships to a wider audience.

## Deferred from: code review of story-1.6 (2026-06-19)

_Three adversarial layers ran at Opus 4.8; the Acceptance Auditor confirmed all 3 ACs + all 8 tasks + all 3 documented deviations (0 critical/high blockers). The two items below are latent — neither is triggered by 1.6's single scene with a valid stub definition; they bite when 1.7's real NavMesh route lands or Epic 5 introduces additive scenes. (Five other findings are tracked as patches / one decision in the story's Review Findings section; the rest were dismissed.)_

- **NavMesh fail-soft guard (`isOnNavMesh`) owed but not yet written** [Assets/Scripts/Events/EventActor.cs] — Task 5 specified "only touch `_agent` when `isOnNavMesh`," but 1.6 has **zero** agent interaction, so the guard was never written and the cached `_agent` field is currently unused (dead). AC3's NavMesh fail-soft is satisfied *vacuously* (no agent code ⇒ nothing to throw). **Action:** when **Story 1.7** introduces the drunk's real route, write the `isOnNavMesh` guard and re-verify the stub/husband spawn point sits on the baked NavMesh. (Entangled with the existing 1.2 NavMesh-agent-dimensions defer and the disabled-stub-agent deviation #1.)
- **Pool doesn't track dispensed (active) instances → orphan risk under additive scenes** [Assets/Scripts/Core/ObjectPool.cs → Clear · Assets/Scripts/Events/EventManager.cs → OnDestroy] — `Clear()` destroys only *idle* instances; an actor that is active (checked-out) when the manager is destroyed isn't reclaimed, and `OnDestroy` doesn't `-=` its `Despawned` handler. **Harmless for 1.6** (single-scene unload destroys both together; Unity GC handles it). **Action:** when **Epic 5 (Home Base)** adds additive scene loading / `DontDestroyOnLoad`, have the pool track dispensed instances (or the manager unsubscribe + reconcile `_activeCount` in `OnDestroy`) so a surviving manager can't pin its cap with a dangling delegate.

## Deferred from: code review of story-1.7 (2026-07-16)

_Three adversarial layers ran at Opus 4.8 over `69b4705..HEAD`. The Acceptance Auditor confirmed AC2 and AC3 fully satisfied; AC1 is structurally proven but never visually verified in-engine (tracked as a decision in the story's Review Findings, not here). Two layers independently rated a pooled-reuse NavMesh snap-back **Critical** — **empirically disproven** in a live Play session (~4 pooled cycles, every respawn at the pub, clean console); the mechanism and the disproof are recorded in the story's Dismissed list so the next reviewer doesn't re-derive it. The six items below are latent: none is triggered by the current straight-line, all-on-mesh route._

- **No `pathStatus == NavMeshPathStatus.PathComplete` guard — an unreachable waypoint stalls route progress silently** [Assets/Scripts/Events/EventActor.cs:191-198] — If `SetDestination` yields `PathInvalid` (a waypoint placed off-mesh, or on a disconnected NavMesh island), `remainingDistance` is `Infinity`, so `remainingDistance <= stoppingDistance` never becomes true and `_waypointIndex` never advances again. The drunk stops wherever the partial path dumps it, with no warning and no watchdog. **Harmless today** (live run showed `pathStatus: 0`/PathComplete). **Action:** add the `pathStatus` term — or a per-waypoint timeout — when **Epic 3 (Event Content Pack #1)** adds routes authored by hand across less forgiving terrain.
- **Waypoint arrival test lacks a `hasPath`/velocity companion — can burn through the route before the path computes** [Assets/Scripts/Events/EventActor.cs:193] — `!_agent.pathPending && _agent.remainingDistance <= _agent.stoppingDistance` is the classic incomplete arrival idiom: `remainingDistance` reports `0` (not `Infinity`) in the window after `pathPending` flips false but before the path is current, so the block can increment and re-`SetDestination` on consecutive frames. **Currently invisible** because the "route the drunk along the road" commit (`d079239`) made every waypoint collinear (all `z=19`, x descending) — burning through waypoints and walking the line look identical. **Action:** add `&& _agent.hasPath` (or a velocity check) **before the first route that turns a corner**, or the drunk will cut it.
- **`HasWaypoints`/`Count` don't null-check array entries** [Assets/Scripts/Events/EventRoute.cs:20,23,35] — An array of size N whose elements are all unassigned returns `HasWaypoints == true`; `GetWaypoint` then falls back to `transform.position` (the route root), so the actor silently walks to an arbitrary point that may be off-mesh. The `OnDrawGizmos` loop `continue`s past nulls, so the Scene view draws a clean path that does not match runtime — the one place a designer would catch this is the place that hides it. **Action:** null-check entries in `HasWaypoints`/`Count`, or draw nulls in red in the gizmo, when routes start being authored by non-devs.
- **`NavUsable` flipping false→true after `Begin()` leaves no destination while `_waypointIndex` increments every frame** [Assets/Scripts/Events/EventActor.cs:191] — If `Begin` runs off-mesh (warned once) and the agent later lands on the mesh, `EnterPhase`'s destination assignment was already skipped, so with no path `remainingDistance` is `0` and the index races to `Count - 1` while the actor stands still; the next phase then heads straight to the last waypoint, silently discarding the route. Movement state is established only in `Begin`/`EnterPhase` and never reconciled when `NavUsable` changes between them. **Action:** reconcile on the false→true transition if runtime NavMesh baking/streaming ever lands (Epic 2 / Epic 5).
- **`NavUsable` going false mid-lifecycle stops movement silently with no re-warn** [Assets/Scripts/Events/EventActor.cs:164] — The off-mesh warning fires only inside `Begin`. If the agent loses the mesh afterwards (streaming boundary, displaced by a collision), all movement ceases with zero diagnostics for the rest of the lifecycle, and `isStopped` keeps whatever the last usable `EnterPhase` set. AC3's "warn and carry on" contract holds only for the Begin-time case. **Action:** fold into the same reconciliation pass as the item above.
- **The agent is never re-disabled on despawn, so the prefab's "ships disabled" only protects the first spawn** [Assets/Scripts/Events/EventActor.cs:152-153] — `Begin` sets `_agent.enabled = true` and nothing ever sets it back; `ObjectPool.Return` only does `SetActive(false)`, which leaves the component flag `true`. From cycle 2 on, `Get()`'s `SetActive(true)` wakes the agent at the **previous despawn point** before the manager repositions it. **Benign today** because the alley is on-mesh (verified: no "Failed to create agent" across ~4 cycles) — but the prefab's disabled-agent trick, and the comment justifying it, are load-bearing only for spawn #1. **Action:** if any future event despawns its actor off-mesh, set `_agent.enabled = false` on despawn (or make the `Warp` in `Begin` unconditional) — otherwise Unity's "Failed to create agent" returns and NFR5's clean-console gate breaks.

## Deferred from: code review of story-1.7 (2026-07-25)

_Second review of 1.7 (three adversarial layers). The headline finding — pooled reuse respawning the drunk at the alley — was **confirmed and patched**, not deferred; it corrects a wrong dismissal from the 2026-07-16 review. The eight items below are real but not blockers for 1.7._

- **Drunk prefab has no Collider** [Assets/Prefabs/Events/EventActor_Drunk.prefab] — the retired `EventActor_Stub` carried a BoxCollider; the real actor carries none. **No current trigger:** grading reads `ISubject.Bounds` (renderer-derived) and there is no `Physics.Raycast`/`SphereCast`/`OverlapSphere` anywhere in `Assets/Scripts`. **Action:** add a collider (or an explicit occlusion proxy) when **Story 1.9** adds the subject-capture gate, if that gate does line-of-sight. Also means the player currently walks through the drunk.
- **NavMesh bake dimensions are metric while world/agent are ~4×** [SampleScene.unity:102-108 vs EventActor_Drunk.prefab:830-843] — same `agentTypeID 0`, but baked `radius 0.5 / height 2 / climb 0.4` against an agent of `radius 1 / height 8`. `climb 0.4` is a ~10 cm step at this scale, so kerbs/thresholds become hard blockers, and the walkable surface is carved with too small a margin for the body. **Action:** re-bake at `radius 2 / height 8 / climb 1.6`, or define a dedicated agent type — decide together with the long-standing world-scale item above.
- **Agent speed 3.5 is Unity's metric default in a ~4× world** [EventActor_Drunk.prefab:832] — ~0.44 m/s human-equivalent; `DrunkWalk` plays at authored speed with `m_ApplyRootMotion: 0`, so the drunk foot-slides continuously through ~20 s of walking phases. **Action:** tune with the world-scale decision.
- **Peak fires ~5–10 units past `WP1_peakSpot`** — the timers own phase advancement, not waypoint arrival, so the money shot lands in open road. Explicitly pre-authorised by the story ("the grade uses the timer, not position — purely visual polish") and worsened by `d079239` re-laying the route without retuning durations. **Action:** either sync peak entry to waypoint arrival, or drop the peak waypoint so nobody believes it is honoured.
- **Terminal-waypoint and despawn edges unhandled** [EventActor.cs:194, :213-219] — arriving early leaves `isStopped == false` with `DrunkWalk` playing (walks in place at the alley mouth); arriving late despawns instantly wherever the actor stands, so the drunk can vanish inside the player's viewfinder. **Action:** on last waypoint set `isStopped` + CrossFade to idle; consider gating despawn on visibility.
- **`husband.fbx.meta` `avatarSetup: 0→1` is dead residue** — the prefab binds Avatar/Mesh to `husband_rigged.fbx`; nothing references the `husband.fbx` avatar. **Action:** revert the import setting or drop it from the File List so it stops reading as current work.
- **Drunk-specific flavour text in shared engine code** [EventActor.cs:165, EventRoute.cs tooltip] — the off-mesh warning says "drunk will run its lifecycle in place"; every route tooltip says pub→alley. `EventActor`/`EventRoute` are the substrate for all of Epic 3's events. **Action:** genericise when the second event type lands.
- **Always-animate + update-when-offscreen don't scale past `maxConcurrent 1`** [EventActor_Drunk.prefab `m_CullingMode: 0`, `m_UpdateWhenOffscreen: 1`] — both were needed to fix the mid-walk freeze and are correct for one actor, but together they skin and evaluate every pooled drunk every frame regardless of visibility. **Action:** revisit when `maxConcurrent` rises above 1 (Epic 2/3).
- **`ThirdPersonCamera` head-bone lookup is a hardcoded path that warns and degrades** [Assets/Scripts/ThirdPersonCamera.cs:43] — pre-existing and unrelated to 1.7 (targets `GameObject.Find("main characters")`, not the husband; the file is not in the 1.7 diff). It silently falls back to a height offset for the first-person camera. **Action:** fold into a future camera-hardening pass.
- **Frame hitch at the Build→Peak boundary can collapse Peak to a single frame** [Assets/Scripts/Events/EventActor.cs Update/EnterPhase] — `EnterPhase` carries the previous phase's negative overshoot and `Update` subtracts raw `Time.deltaTime`, so a hitch ≥ the 1.5 s peak duration can fire `Advance()` the very next frame — defeating `EventDefinition.IsValid`'s `peak.duration > 0` rule, whose stated purpose is that a poll-based grader must never miss the peak. **Attempted and reverted 2026-07-25:** clamping the input (`Mathf.Min(Time.deltaTime, 0.1f)`) decouples the FSM clock from wall-clock — every frame slower than the clamp stretches the lifecycle instead of keeping time, and in a low-fps editor the 24.5 s event ran well past a minute. **Action:** if revisited, clamp how much negative `_timer` `EnterPhase` carries forward (e.g. `Mathf.Max(_timer, -phase.duration * 0.5f)`), not `Time.deltaTime` on the way in — that bounds phase-skipping without slowing the timeline.

## Deferred from: code review of story-1.8 (2026-07-25)

_Three adversarial layers ran at Opus 5 (2 decisions, 10 patches, 4 deferred, 4 dismissed). Notably, three of
the Blind Hunter's four High findings were disproved on verification — that layer is denied project access by
design, so its Highs are hypotheses. The four items below are real but latent: none is triggered by the
current single-definition, single-prefab setup._

- **Pool-reset invariant covers only five audio fields** [Assets/Scripts/Events/EventActor.cs:236-275] — `Begin()`'s comment asserts "Assume nothing came back from the pool; state it all again", but only `clip`, `loop`, `minDistance`, `maxDistance` and the rolloff/curve are re-applied. `volume`, `mute` and `dopplerLevel` are not. **No current trigger** — nothing mutates them. **Action:** the moment a fade-out despawn subscriber lands (the code's own comment at `:335-339` anticipates one), it will ramp `volume` to 0 before pooling and every reuse after the first will be permanently silent with a clean console. Re-apply `volume = 1` in `Begin()` when that work starts.
- **Editor tool reflection hardening for future Unity versions** [Assets/Scripts/Editor/CreateGameAudioMixer.cs:133-141,174-187,203-210] — overloads are chosen by `FirstOrDefault(m => m.Name == ...)` with no signature filter (CLR makes no ordering guarantee, and the correct pattern is used ten lines earlier for `SanitizeGroupViews`); `Invoke`'s padding special-cases `bool` but passes `null` for any other value type, which `MethodInfo.Invoke` rejects; `DumpApi`'s `Contains("Group") || Contains("Snapshot")` filter excludes `CreateMixerControllerAtPath`, the very method whose absence it exists to diagnose; and that method alone is looked up with `BindingFlags.Public` while the file's own doc-comment calls the API internal. **Action:** harden only if a Unity upgrade breaks the tool. `GameAudio.mixer` is already committed and the tool is a regeneration convenience, not a runtime dependency — its blast radius is one developer's afternoon, not the game.
- **`spatialBlend` warning reads the pan curve only at distance 0** [Assets/Scripts/Events/EventActor.cs:166-167] — `AudioSource.spatialBlend`'s getter samples `panLevelCustomCurve` at distance 0, so a rig authored to ramp 2D-near → 3D-far warns spuriously, while the genuinely broken shape (1.0 at the source, dropping to 0 in the far field) is not detected. **No such rig exists** — the prefab's curve is a constant 1. **Action:** if distance-varying spatial blend is ever authored, sample the curve across the range instead of trusting the scalar getter.
- **A pooled actor serving a different definition would leak rolloff state** [Assets/Scripts/Events/EventActor.cs:269-274] — the cue-less `else` branch clears `clip` but leaves `rolloffMode`, the custom curve, `minDistance` and `maxDistance` from whatever definition ran last. Reported independently by two layers as a live pooling bug; it is **structurally impossible today** because `definition` is a serialized prefab field with no runtime setter (`EventActor_Drunk.prefab:856`), so one actor instance is permanently bound to one definition. **Action:** if Epic 3 introduces a runtime `definition` setter so one prefab can serve several event types, this becomes real immediately — reset the full audio config in `Begin()` at that point. (Patch 1 fixes the same code path for the accent-only case, which does *not* require pooling to trigger.)

## Deferred from: play-mode verification of story-1.9 (2026-07-26)

_Found by photographing the real capture path (`Tools > Grading > Photo Shoot (Play)`) rather than
asserting against expectations. Reviewed by Alexv, who accepted the shots._

- **The ~8% frame-coverage gate looks too strict for this character** [Assets/Data/Grading/GradingConfig.asset · `minCoverage`] — at a natural portrait distance (~20 world units) the *animated* drunk measures **4.14%** and the shot is rejected, although he is unmistakably the subject of the photograph (see `Temp/PhotoShoot/b_mid.png`). At ~50 units on full zoom he sits exactly on the threshold — 7.34% on one run, 9.02% on the next, decided purely by which frame of the walk cycle the shutter caught. Root cause is not the threshold alone: coverage is measured as the **area** of an axis-aligned box around a tall, thin, arms-down figure, which is mostly empty air, so it under-reads him badly. **Action:** revisit in **Story 1.10** when the real composition curve lands and re-tunes these numbers anyway. Two candidate fixes: lower `minCoverage` to ~3%, or score the box's **height** fraction instead of its area (far less sensitive to a narrow silhouette, and closer to how a photographer judges "he fills the frame"). Do not tune this by arithmetic — run the photo shoot and look at the pictures.
- **Grading measures a bounding box, not the subject** [Assets/Scripts/Grading/ShotGrader.cs] — the AABB over-reports a figure standing diagonally and under-reports one standing straight on, and it breathes with the animation (the same pose at the same distance can land either side of the gate). Acceptable for the MVP — the architecture chose AABB deliberately for cheapness — but it is the underlying reason the threshold feels arbitrary. **Action:** if grading ever feels unfair in playtest, this is the thing to change, not the number. An oriented box or a few silhouette sample points would both be more faithful.

## Deferred from: code review of story-1.9 (2026-07-26)

_Three adversarial layers ran (Blind Hunter, Edge Case Hunter, Acceptance Auditor) plus a runtime probe of
`ShotGrader`'s projection and gate maths. 50 raw findings deduped to 32: 2 decisions, 21 patches, the 5
items below, 4 dismissed. The probe closed two open evidence gaps — the near-plane clipping is correct for
straddling and behind-camera subjects, and NFR2 measures 0.0072 ms/call against the real town's 16,739
colliders (~28,000x margin). Note the ~8% gate item is already logged in the section above from the dev
session; the review corroborated it with independent measurements (5.19% at 20u, 11.81% at 14u for a clean
box) and Alexv confirmed on 2026-07-26 that it holds for Story 1.10, which re-tunes these numbers with the
real composition curve. The review also found that at this tuning the occlusion gate is nearly unreachable
in the real game — coverage rejects first — so retuning is what makes AC1's occlusion half testable._

- **Star mapping is degenerate while the score is raw coverage** [Assets/Scripts/Grading/ShotGrade.cs · `Stars`] — `Stars => Clamp(CeilToInt(Percent01 * 5), 1, 5)` fed by raw coverage means a typical real hit (16-21%) is 1-2★ and 5★ demands ~80% of the screen, which an axis-aligned box round a thin figure only reaches at point-blank range. A miss and an ordinary good shot are both 1★. **Action:** Story 1.10 replaces the score body; the seam is already commented at the exact line. **Do not let Story 1.12's HUD gate on `Stars`** until 1.10 lands.
- **`TagManager.asset` lost every URP rendering-layer name** [ProjectSettings/TagManager.asset] — adding the `Subject` layer via the editor API triggered Unity 6's serialization migration, which bumped `serializedVersion` 2→3 and truncated `m_RenderingLayers` from 32 entries to just `Default`, deleting `Light Layer 1`-`Light Layer 7`. **Verified harmless today:** every `m_RenderingLayerMask` in the project is `1` and both scene cameras' culling masks are all-bits, so only the Inspector labels are gone. **Action:** none required; restore the names if light/decal layers are ever used. Logged so it is a known change rather than a future surprise.
- **Subjects can never occlude each other** [Assets/Prefabs/Events/EventActor_Drunk.prefab · Assets/Data/Grading/GradingConfig.asset] — the whole actor hierarchy (28 objects) moved to layer 8 `Subject` while `occluderMask` is Default-only, so a subject standing directly behind another grades as fully visible and the shot counts for the hidden one. The actor has also left `Default` for *every* other mask in the project (raycasts, physics layer matrix, culling) with no survey of those call sites. **Action:** revisit in **Epic 2** when `maxConcurrent > 1`; invisible today.
- **The miss reason cannot reach a release build** [Assets/Scripts/Grading/ShotGrader.cs · `GradeDetail`] — `ShotCapturedChannel` still carries only `ShotGrade`; the reason lives in `GradeDetail`, exposed solely via `PhotoModeController.LastDetail` behind `#if UNITY_EDITOR || DEVELOPMENT_BUILD`. Outside the editor nothing but the log line knows why a shot missed. The story permitted this ("adding a miss-reason to the struct is *allowed*"), so it is not a violation. **Action:** **Story 1.12** will need the reason for its feedback HUD — expect to widen `ShotGrade` or the channel payload then.
- **Edit-mode `BuildNavMesh()` may not survive the play-mode domain reload** [Assets/Scripts/Editor/PhotoShootRig.cs:115-117] — `NavMeshSurface.BuildNavMesh()` called in edit mode produces non-persisted `NavMeshData`; a scene reference to a non-persisted object is not guaranteed to survive the reserialization that entering play mode causes. If it does not, the rig silently reproduces the "no valid NavMesh" agent errors it was written to eliminate and photographs a stationary subject — which changes every coverage number the run exists to judge. **Not independently verified:** the story's recorded run shows animated bounds and a clean console, which suggests it does survive, but I did not re-run the shoot. **Action:** confirm on the next photo-shoot run; if it proves flaky, bake to a persisted asset or call `BuildNavMesh()` from the runner's `Start()` in play mode.

## Deferred from: story-1.10 placement study in the real town (2026-07-26)

_Found while re-shooting the centred-vs-thirds comparison against real scenery (`Tools > Grading > Photo
Shoot — Placement Study (Town)`, output `Temp/PhotoShootTown/`) after Alexv's perceptual review. Not a
Story 1.10 defect — 1.10 does not touch the occlusion gate — but it was reproduced 24 times with
photographs and it undermines Story 1.9's AC1 in the real game, so it must not be lost._

- **Nothing in the real town occludes the subject: 24 of 24 shots reported `line-of-sight 100%`, several with the drunk entirely invisible behind a pine tree or a terrain hill** [Assets/Scripts/Grading/ShotGrader.cs · `VisibleFraction` · Assets/Data/Grading/GradingConfig.asset · `occluderMask`] — `Temp/PhotoShootTown/dir000_centred.png` is the clearest example: the grader drew its box over a tree trunk, reported a clear view, and scored the photograph **98% / 5★** with no subject visible in it at all. This is the same *class* of bug as 1.9's "5 stars for a photograph of nothing", one gate along.
  **What was ruled out:** the world objects are on layer 0 `Default`, which **is** in `occluderMask` (`m_Bits: 1`), and they **do** carry MeshColliders (16,738 of them in the scene). So it is neither a layer-mask mistake nor missing colliders in general.
  **Not yet diagnosed** — candidate causes, in order of my suspicion: (a) the rig parks the camera blindly at a fixed offset and may be spawning it *inside* a tree's MeshCollider, and `Physics.Linecast` does not report a collider it starts inside; (b) the specific foliage meshes in frame may be among the objects the `Add MeshColliders to World` tool skipped; (c) non-convex MeshCollider back-face behaviour. **Label this a reproduced symptom, not a diagnosed defect.**
  **Action:** investigate before Story 1.12's HUD ships, because a 5★ awarded for a photograph of a tree is exactly what the player will screenshot and complain about. The cheapest decisive probe: at each rig vantage point, log `Physics.RaycastAll` from the camera to the subject and print what it actually hits — if the list is empty, it is (b); if it contains the tree only when the camera is moved back, it is (a).
- **The photo-shoot rig places its camera without checking whether the subject is visible from there** [Assets/Scripts/PhotoMode/PhotoShootRunner.cs · `PlaceCamera`] — in the private world's empty plane this never mattered; in the real town two of the first four vantage points photographed a tree. The 12-direction sweep works around it by producing enough shots that some are usable, and the story's own rule ("suspect the rig") caught it — but any future rig work in a real scene should verify the subject is actually in the picture rather than assuming a clear line. **Action:** if the town rig gets reused, add a visibility check (a pixel probe inside the graded box, or the raycast above once it is understood) and skip or re-aim vantage points that fail it.

## Deferred from: code review of story-1.10 (2026-07-28)

_Three parallel review layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) over the shipping game
code only — the photo-shoot rig was excluded from scope at Alexv's direction as test scaffolding. Items
below are real but not caused by Story 1.10, or not actionable now. Six further findings were dismissed
as disproven; they are recorded in the story file so a future review does not re-raise them._

- **Town occlusion — see the 2026-07-26 entry above; this review reproduced it and narrowed candidate (b)** [Assets/Scripts/Grading/ShotGrader.cs · `VisibleFraction`] — confirmed again from `_bmad-output/verification/photo-shoot-town/shots.txt`: **24 of 24 shots read `line-of-sight 100%`**. Adding to the existing diagnosis: `Tools > Add MeshColliders to World` (`Assets/Scripts/Editor/AddMeshColliders.cs`) only walks `MeshFilter`s beneath a root named **`"game map"` or `"World"`** — anything outside that root received no collider at all, which would make candidate (b) structural rather than incidental. **The cheapest decisive probe is unchanged** (log `Physics.RaycastAll` from camera to subject at each vantage point). Note this also means AC4's claim that "the occlusion half is no longer inert" holds in the private rig world (`u_wall_mid` correctly rejects `Occluded`) but is **contradicted in the real town** — do not treat AC4's occlusion half as proven in the shipping scene.
- **`TryGetConfigProblem` reports only the first problem, once at Awake** [Assets/Scripts/Grading/GradingConfig.cs:250-415] — Story 1.10 added ten checks to a first-wins chain, and `minSubjectHeight <= 0` returns before every composition-curve and "five stars unreachable" check. A designer with three authoring mistakes needs three play-mode cycles to discover them. **Action:** accumulate all problems into one warning when the config surface next grows.
- **A NaN phase duration passes `EventDefinition.IsValid`** [Assets/Scripts/Events/EventDefinition.cs:122] — pre-existing (Story 1.6). `NaN < 0f` is false, so a hand-authored `duration: .nan` validates clean; `EventActor.PeakOffset` then returns NaN and every post-peak shot scores 0% with a clean console. **Action:** add a finiteness check alongside the negative-duration check.
- **The "five stars unreachable" validation family does not cover the sweet-spot vs cut-off interaction** [Assets/Scripts/Grading/GradingConfig.cs:385-401] — hypothesis. `heightFraction` is viewport-clamped to 1.0, so a sweet spot authored very high (e.g. `prominenceIdealMin: 0.95`) requires the subject to nearly fill the frame, at which point `framedFraction < 1` drags composition permanently below 1.0. Silent, clean console, caps every photograph — the exact class Story 1.10 asked to be warned about, one interaction further out. **Action:** fold into the validator when composition next changes.
- **NFR2's 0.0072 ms figure was re-asserted, not re-measured** [Assets/Scripts/Grading/ShotGrader.cs:123-124] — the argument is sound (I confirmed no new linecast, allocation or GPU readback in the 1.10 additions) but the measurement was not re-taken. **Action:** re-measure whenever grading gains anything that is not pure arithmetic.
- ~~**`ShotGrade.FromPercent` is now dead code and contradicts its own struct's documented invariant**~~ — **RESOLVED by Story 1.12 (2026-08-07): deleted.** Zero production callers were re-confirmed across `Assets/` before removal. Deleted rather than made non-`Counted`, because no caller genuinely has only a total: anything that scores a shot has the axes (that is what `Scored` takes) and anything that does not has a miss (`Missed`), so a third factory would only be a way to construct the all-zero-breakdown `Counted` state that Story 1.12's AC2 forbids. A comment stands where it was, saying so.
- **The placement penalty is aspect-distorted** [Assets/Scripts/Grading/ShotGrader.cs:363-368] — `cx`/`cy` are normalized independently by viewport width and height, so on a 16:9 frame the same pixel offset costs roughly 1.8x more vertically than horizontally. This is standard practice for normalized viewport space and may well be what you want; it is simply undocumented. **Action:** none unless placement ever feels wrong on an unusual aspect ratio — then this is the first thing to check.

## Deferred from: code review of story-1.11 (2026-07-30)

_Three parallel review layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) over the shipping
game code only — `GalleryShootRig.cs` and `GalleryShootRunner.cs` were excluded as test scaffolding at
Alexv's standing direction; the rig was used to gather evidence, not reviewed. Items below are real but
not actionable now, or not caused by Story 1.11. Four further findings were disproven by running them
and are recorded in the story file so a future review does not re-raise them._

- **`RaiseSuppressed` is a bool, not a refcount or owner token** [Assets/Scripts/PhotoMode/PhotoModeController.cs:284] — the doc-comment explicitly invites multiple callers ("Story 1.12's HUD, a pause menu or a map screen would use the same switch"), but any caller's `SetRaiseSuppressed(false)` clears every other caller's suppression. Harmless today with exactly one caller. **Still deferred after Story 1.12 (2026-08-07): 1.12 deliberately declined to become the second caller** — `GradeHud` only *reads* `IsPhotoMode` and calls nothing on `PhotoModeController` (decision 1b), so the gallery remains the sole suppressor. **Action:** unchanged — a refcount or owner token, required before whatever genuinely does become the second caller (a pause menu is the likeliest next candidate).
- **The cell pool is sized from storage capacity, not from what can ever be displayed** [Assets/Scripts/Gallery/GalleryView.cs:182] — `BuildCells` constructs `capacity` cells at `Awake`, each 3 GameObjects plus a `RawImage` and a `Text`, whether or not the gallery is ever opened. 150 GameObjects at the shipped `maxStoredShots: 50`; **1,500 at the permitted maximum of 500**, all under one `GridLayoutGroup` that `Canvas.ForceUpdateCanvases()` walks twice per open. The pool only ever needs `fitCols * fitRows`. **Action:** size the pool from what fits and refill on open, if the cap is ever raised or the gallery gains scrolling.
- **An `EventDefinition` with an empty `Id` captions a counted shot "nobody"** [Assets/Scripts/Events/EventDefinition.cs] — pre-existing: `Id` is a plain `public string` with a tooltip saying "Must be non-empty" and no validation anywhere. `EventActor.SubjectId` falls back to `definition.Id`, so an unauthored id makes a genuine hit indistinguishable from `GradeMiss.NoSubject` in the gallery, and `CapturedShot`'s documentation asserts empty means "there was no subject, never unknown" — an assertion Epic 5 will serialize to JSON. **Action:** validate `Id` non-empty on `EventDefinition`, alongside the deferred NaN-duration check on the same asset.
- **`BytesPerPixel = 3` may understate the real reservation by roughly 2×** [Assets/Scripts/Gallery/GalleryConfig.cs:88] — hypothesis, **not measured**. `tex.Apply()` is called with `makeNoLongerReadable: false` (correct, and required for Epic 5's `EncodeToPNG`), so each thumbnail plausibly keeps both a CPU-side RGB24 copy and a GPU-side texture, and most GPUs pad RGB24 to 4 bytes. If so, `MemoryWarnBytes` fires at about twice the intended threshold. **Action:** measure with the Memory Profiler before trusting the budget figure; do not "fix" the arithmetic on the strength of this note alone.
- **The memory-budget guard is the one branch the boundary run never entered** [Assets/Scripts/Gallery/GalleryConfig.cs:200] — `TryGetConfigProblem` returns on the first problem found and the `WorstCaseBytes > MemoryWarnBytes` check is last, so every "huge" value the rig tried tripped an earlier range check first. A blown budget is reachable with every field *in range* (e.g. 1080 × 1920 × 200 ≈ 1.2 GB), and no scenario exercised that combination. The guard aimed squarely at NFR3's authoring failure mode is the one that was never run. **Action:** add an all-in-range-but-over-budget scenario next time the rig runs.
- **A cap raised at runtime lets the header report shots it is not drawing** [Assets/Scripts/Gallery/GalleryView.cs:378] — `slots = _cells.Count` is fixed at `Awake` while the service re-reads the cap live on every capture. Raise `maxStoredShots` during play and `count` exceeds the pool: `LayoutGrid` returns `count` (it knows nothing about `slots`), so the `shown < count` branch is skipped and the header reads "60 shots (holds 50)" while drawing 50. The one case the header exists to make loud is the one it gets wrong. **Action:** clamp `shown` to `slots`; folds naturally into the pool-sizing item above.
- **`TryGetConfigProblem` reports only the first problem; `_warnedAspect` latches for the session** [Assets/Scripts/Gallery/GalleryConfig.cs:158, GalleryService.cs:258] — a config with two authoring mistakes needs two play-mode cycles to discover both, and `OnValidate` can destroy the second one's evidence in between. The aspect warning latches on the first clamping aspect and never records which aspect it warned about, so a later resize diverges silently. **This mirrors the identical deferred item against `GradingConfig` from the story-1.10 review** — fix both together when the config surface next grows.
- **The saved scene says World Space render mode; `ConfigureCanvas` overrides it at `Awake`** [Assets/Scenes/SampleScene.unity] — the YAML has `m_RenderMode: 2`, `m_Camera: {fileID: 0}`, `m_SortingOrder: 0`, while Task 5 required Screen Space – Camera with a high sorting order. Runtime behaviour is correct and was photographed, but anyone reading or re-authoring the scene sees World Space. **Action:** author the scene to match the code, so the file stops disagreeing with the running game.
- **`MakeId`'s `D4` index stops sorting lexicographically past 9,999 shots in one session** [Assets/Scripts/Gallery/CapturedShot.cs:116] — `_shotCounter` increments on every capture, including evicted ones, and is never reset. Past 9,999 the ids widen and the "naturally chronological sort for free" that the class comment sells to Epic 5 quietly stops holding. **Action:** widen the format or reset per session when Epic 5 makes ids filenames for real.
- **The gallery shows only what fits on one screen — 18 of 50 stored shots are unreachable** [Assets/Scripts/Gallery/GalleryView.cs:372] — **deferred by Alexv's ruling on 2026-07-30: accepted as MVP scope for a story explicitly called "minimal gallery", because the header is honest about the loss.** `LayoutGrid` shrinks cells to fit and shows only what fits; `Refresh` is the sole fill path and runs only on `Open()`. Evidence: `_bmad-output/verification/gallery/ui_open_50shots.png` reads "GALLERY — 50 shots · showing newest 32", confirmed by `gallery.txt:170`. At the permitted `maxStoredShots: 500` the great majority would be unreachable. The GDD calls the gallery "the payoff, not a debug list" (Pillar 3). **Action: raise a scrolling/paging gallery as an Epic 5 story** — and note the cell-pool sizing item above folds into it.
- **The gallery is look-only: no way to select a shot or view it full-screen** [Assets/Scripts/Gallery/GalleryView.cs] — **not a defect; confirmed out of scope for Story 1.11.** Neither the story nor `epics.md` mentions selecting, opening or enlarging a photograph; AC2 asks only that the player can open the gallery and see the shots with their star grades, which it does. Noticed by Alexv on 2026-07-30 while doing the window-resize check. **Action: fold into the same Epic 5 story as the scrolling/paging item above** — "the gallery becomes browsable" is one piece of work: scroll to reach all of them, click to inspect one. Worth noting the groundwork is already there: cells are pooled with stable indices, `CapturedShot.Image` is a full readable `Texture2D` (not a downscaled preview), and every cell's `RawImage` already has `raycastTarget = false` — flipping that on the cell is where a click handler would start.

## Deferred from: play-mode verification of story-1.12 (2026-08-07)

_Found by photographing the real HUD in the real scene (`Tools > HUD > Grade HUD Shoot (Play)`,
output `_bmad-output/verification/hud/`) rather than asserting against expectations. Neither item is a
Story 1.12 defect; both are raised because 1.12's own evidence is where they became impossible to ignore._

- **The town occlusion symptom is now the most visible thing in the player's own feedback** [Assets/Scripts/Grading/ShotGrader.cs · `VisibleFraction` · Assets/Data/Grading/GradingConfig.asset · `occluderMask`] — `_bmad-output/verification/hud/a_money_shot.png` shows the readout stating **`★★★★★ 94 %`, `seen 100 %`, `right on the moment`** over a photograph in which the drunk is behind a pine tree and not visible at all; the grader's box is drawn on the tree. This is the same reproduced-but-undiagnosed symptom already logged twice (2026-07-26 placement study: 24 of 24 shots read `line-of-sight 100%`; corroborated by the 1.10 review), and the existing entry says outright it should be investigated "before Story 1.12's HUD ships, because a 5★ awarded for a photograph of a tree is exactly what the player will screenshot and complain about". **Story 1.12 was instructed to raise this as a scope question rather than silently expand to include it, so it is NOT fixed.** What has changed is the blast radius: until now the wrong verdict lived in a log line and a gallery caption; it is now a full-width readout telling the player, in words, that they took a great photograph of a tree. **Action: Alexv's call whether this becomes its own story before Epic 2.** The cheapest decisive probe is unchanged — log `Physics.RaycastAll` from camera to subject at each vantage point and print what it actually hits. Note the strongest existing lead: `Tools > Add MeshColliders to World` only walks `MeshFilter`s beneath a root named `"game map"` or `"World"`, so foliage outside that root would have no collider at all.
- **`hideOnCameraLowered` is a feel decision nobody has played yet** [Assets/Scripts/UI/GradeHudConfig.cs · Assets/Data/UI/GradeHudConfig.asset] — `RaiseCamera` is a *hold*, so with this on (the default) releasing the button takes the grade off screen instantly and a player who clicks and lets go may never read it. With it off, an Overlay readout can linger over the Screen Space – Camera gallery for up to 2.8 s. Story 1.12 shipped it as a switch precisely because both readings are defensible and only playing settles it; the structural consequences of both are photographed (`stress_camera_lowered.png`, `stress_gallery_over_hud.png`). **Action: fold the answer into the config default once Alexv has played it.** If "off" wins, the gallery overlap needs a different fix and `GalleryView.Open` is the natural place — it already suppresses the camera and the player, and one more thing to suppress is the same shape.
